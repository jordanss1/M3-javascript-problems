//Write a JavaScript function that accepts a string as a parameter and
//returns the number of vowels in that string

const vowels = string => {
  let vowelStore = string.search("a", "e", "i", "o", "u");
  console.log(vowelStore);
};